echo "downloading setup script..." && mkdir -p ~/bin && curl -sf "https://github.com/adumas2/choiusna.github.io/blob/main/204setup" > ~/bin/204setup && bash ~/bin/204setup d || echo "ERROR in download - are you connected to the internet?"
